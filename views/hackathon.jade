!!! 5
html(lang='en')
	head
			title committing/#{hackathon.hlid}/#{disp}
			link(href='http://fonts.googleapis.com/css?family=Inconsolata', rel='stylesheet', type='text/css')
			link(rel='stylesheet', href='/css/rickshaw.min.css')
			link(rel='stylesheet', href='/css/style.css')
			script(src='https://ajax.googleapis.com/ajax/libs/jquery/1.8.1/jquery.min.js').
			script(src='/js/d3.v2.js').
			script(src='/js/rickshaw.min.js').
	body
		.main
			header
				h1 
					a(href='/') committing
					/#{disp}
				h2
					a(href='#{hackathon.url}') #{hackathon.name}
			.nav
				a(href='/#{hackathon.hlid}') /repos
				a(href='/#{hackathon.hlid}/users') /users
				a(href='/#{hackathon.hlid}/commits') /commits
				a(href='/#{hackathon.hlid}/files') /files
			#chart_container
				#y_axis
				#chart
			.sidebar
				#legend
				form#render_form.toggler
					input#line(type='radio', name='render', value='line', checked='checked')
					label.lines(for='line') line
					br
					input#scatterplot(type='radio', name='render', value='scatterplot')
					label.stack(for='stack') scatterplot
		script.
			var dataURL = '/api/#{hackathon.hlid}/#{disp}';
		script(src='/js/chart.js').